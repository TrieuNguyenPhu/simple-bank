services:
  postgres17:
    image: postgres:17-alpine
    container_name: postgres17
    environment:
      - POSTGRES_USER=trieunguyen
      - POSTGRES_PASSWORD=trieu080604
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: